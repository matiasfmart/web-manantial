services:
  - type: web
    name: iglesia-manantial-web
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
    autoDeploy: true
